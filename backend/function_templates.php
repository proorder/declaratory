<?php
  //---------------------------------------
  // Router
  //---------------------------------------
  $route = explode('/', $_SERVER['REQUEST_URI']);
  if ($route[1] != 'api') {
  } else {
    'api_'.$route[3]();
  }

  //---------------------------------------
  // Hash function template
  //---------------------------------------
  function api_ ($params) {
    // Запрос в BD, добавить в аргументы параметры запроса
  }
?>
